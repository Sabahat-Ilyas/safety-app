{
  "name": "safety-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm install --prefix client && npm run build --prefix client && mkdir -p public && cp -r client/dist/* public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "libphonenumber-js": "^1.12.38",
    "nodemailer": "^8.0.5",
    "twilio": "^5.12.2"
  }
}
