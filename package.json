{
  "name": "safety-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
